#
# define your domain to be the blue shades
#
#color.source="blue" if ($fields[0]=~/seven.ch/);
#color.source="red"
#color.target="lightblue3" if ($fields[1]=~/seven.ch/);
#color.target="orchid1"
#
# An example to check for an open relay
#
#color.source="red" if (($fields[0] !~ /seven.ch/) && ($fields[1]!~/seven.ch/))
#color.source="invisible"
#color.target="orchid1" if (($fields[0]!~/seven.ch/) && ($fields[1]!~/seven.ch/))
#color.target="invisible"
#
# Anyone getting big emails?
# config: from to size
#
#color.source="red" if ($fields[2] > 100000);
#color.source="invisible"
#color.event="red" if ($fields[2] > 100000);
#color.event="invisible"
#color.target="red" if ($fields[2] > 100000);
#color.target="invisible"
#
# Config: from delay
#
#color.source=$fields[1]=~s/://g; "orange" if ($fields[1]>200);
#color.source="invisible"
#color.target=$fields[1]=~s/://g; "firebrick" if ($fields[1]>1000);
#color.target=$fields[1]=~s/://g; "red" if ($fields[1]>200);
#color.target="invisible"
